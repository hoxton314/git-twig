{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Allows checking and installing updates",
  "windows": ["main"],
  "permissions": [
    "updater:default",
    "process:allow-restart",
    "dialog:allow-message"
  ]
}
